rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /branches/{branchKey}/people/{personId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
